{
  "name": "cf_ai_supportbot",
  "version": "1.0.0",
  "description": "AI-powered support bot built on Cloudflare infrastructure",
  "main": "workers/api.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "test:coverage": "vitest --coverage",
    "build": "tsc",
    "pages:dev": "wrangler pages dev pages/dist",
    "pages:deploy": "wrangler pages deploy pages/dist"
  },
  "keywords": ["ai", "chatbot", "cloudflare", "workers", "llama"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "wrangler": "^3.78.0"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}